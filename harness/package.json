{
  "name": "agon-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Replay harness / Agent CI. Replays an agent deterministically over a fixed historical window and produces a verifiable performance report to anchor on-chain.",
  "scripts": {
    "replay": "tsx src/replay.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
